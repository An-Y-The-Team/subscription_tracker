[pytest]
# Put the backend root on sys.path so tests can `from app.main import app`
pythonpath = .
testpaths = tests
# Treat `async def test_*` and async fixtures as asyncio without per-test markers.
asyncio_mode = auto
