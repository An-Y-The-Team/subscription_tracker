# Dev/CI-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
ruff>=0.5.0
pytest>=8.0.0
pytest-asyncio>=0.23.0  # async test + fixture support
aiosqlite>=0.19.0  # async SQLite driver for in-memory test DB
httpx>=0.27.0  # required by Starlette/FastAPI TestClient
